# Design System Rules

- Keep design-system stories in `components/design-system`.
- Use `foundations` for tokens and usage guidance.
- Use `patterns` for multi-component composition examples.
- Keep examples neutral and starter-oriented, not product-specific.
- If token definitions change in `app/globals.css`, update `components/design-system/foundations/Tokens.stories.tsx` in the same task.
