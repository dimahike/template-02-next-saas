# Features Rules

- Feature components compose reusable primitives and app-specific state.
- Keep data access behind `lib/api` and `lib/server`.
- Do not import server-only modules into client components.
- Keep feature stories colocated with features (for example `components/features/**/FeatureName.stories.tsx`).
