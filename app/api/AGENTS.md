# API Route Rules

- Keep route handlers thin and typed.
- Validate external input before use.
- Do not couple route handlers to product-specific logic in starter baseline.
- Move reusable server logic into `lib/server`.
